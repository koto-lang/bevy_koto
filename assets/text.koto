# A 'hello world' example for the text plugin

export
  on_load: |state|
    state.text =
      make_text("Hello, World!")
        # TODO - How to scale text in a more useful way?
        .set_size 0.001
